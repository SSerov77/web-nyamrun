services:
  web:
    build: .
    # Без depends_on: nginx!
    command: ...
    volumes:
      - .:/app
    expose:
      - "8080"

  nginx:
    image: nginx:1.25
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
    depends_on:
      - web 